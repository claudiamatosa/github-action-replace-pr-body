
name: "unit tests"
on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: johnteske/github-action-replace-pr-body@master
      with:
        github-token: "${{ secrets.GITHUB_TOKEN }}"
